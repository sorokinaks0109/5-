# Скопируйте этот файл в .env.local и заполните, чтобы включить боевой режим с Supabase.
# Если переменные пустые — приложение работает в демо-режиме (данные хранятся в браузере).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
