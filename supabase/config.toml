# Настройки для Supabase CLI (используются при публикации функции через GitHub Actions).
[functions.game]
# Проверку входа функция делает сама: так работают и новые, и старые ключи Supabase.
verify_jwt = false
