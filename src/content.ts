// Публичная часть content.json: тексты, названия, настройки. Без заданий и ответов.
import pub from 'virtual:public-content';
import type { PublicContent } from './core/types.ts';

export const pc = pub as PublicContent;
